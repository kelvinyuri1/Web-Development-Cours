import { useContext } from "react";
import { ValueContext } from "../contexts/ValueContext";

export function AddButton() {
  const { setUpValue } =
  return (
    <>
      <button>Add+</button>
    </>
  );
}
